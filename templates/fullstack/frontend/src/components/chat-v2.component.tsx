import { Button, Divider, Input, List, Typography } from 'antd'
import React from 'react'

import { sleep } from '../utils'
import { HTTPClient } from '../utils/http'

export const ChatV2: React.FC = React.memo(() => {
  // websocket....
})
